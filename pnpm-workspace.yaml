packages:
  - "."

# The application and test aliases need independent native binaries because
# Electron and Node use different ABIs.
nodeLinker: hoisted
packageImportMethod: copy

allowBuilds:
  better-sqlite3: true
  electron: true
  electron-winstaller: true
  esbuild: true
